function login( username, password){
    if (username==="admin" && password==="password123") {
        return"Login successful!"
        
    }else{
        return "invalid usermane or password"
    }
}

function login( username, password){
    if (username==="admin" && password==="password123") {
        return"Login successful!"
        
    }else{
        return "invalid usermane or password"
    }
}